# I-Wish Custom Overrides

This directory is reserved for project-level and user-level overrides.

- Keep team overrides in versioned files when they are safe to share.
- Keep personal overrides in ignored files when they contain user-specific preferences.
- Installer updates must preserve everything under this directory by default.
