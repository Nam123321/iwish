---
name: 'code-search'
description: 'Local hybrid semantic+lexical code search using Semble. Run query locally on CPU.'
---

# /code-search

Exposes the local hybrid semantic+lexical code search tool to find code snippets by natural language intent.

## Usage
Run via:
```bash
python3 .agent/skills/code-search/scripts/code-search.py --query "<query>" --path .
```
